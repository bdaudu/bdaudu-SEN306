1a. I achieved functional cohesion by ensuring that each  functions created are relevant to the program and that each function performs one main task necessary for the code to work seamlessly.

b(i). validateOrder() -  Validates the customer's order data
(ii). calculateOrderTotal() - Calculates the sum of orders
(iii). determineDiscountRate() - Dertermine discount
(iv). createCustomerMessage() - Creates the customer message
(v). notifyCustomer() - Displays and sends message to the customer
(vi). updateCustomerBalance() - Updates the balance of the customer
(vii). sendEmail() - Handles email sending

2. Pass by value-result

3. Through pass-by-value-result,
a. The value of balance is copied into d when the call begins.
b. The method executes and modifies d.
c. When the method returns, the final value of d is copied back to balance.